apiVersion: v1
kind: PersistentVolume
metadata:
  name: expense-pv
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: /mnt/expense-data